{{page-title "Console"}}
<Layout::Container>
    <Layout::Header @user={{this.user}} @organizations={{this.organizations}} @extensions={{this.extensions}} @onAction={{this.onAction}} />
    <Layout::Main>
        <Layout::Sidebar @onSetup={{this.setSidebarContext}}>
            <div class="next-sidebar-content-inner">
                <div role="menu" id="sidebar-menu-items"></div>
            </div>
        </Layout::Sidebar>
        <Layout::Section>
            {{outlet}}
        </Layout::Section>
    </Layout::Main>
    <Layout::MobileNavbar @extensions={{this.extensions}} />
</Layout::Container>